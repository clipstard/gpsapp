import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gpsapp',
  appName: 'gpsapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
